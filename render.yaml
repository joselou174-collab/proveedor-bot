services:
  - type: worker
    name: proveedor-bot
    runtime: python
    envVars:
      TELEGRAM_TOKEN:
        fromSecret: TELEGRAM_TOKEN
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
